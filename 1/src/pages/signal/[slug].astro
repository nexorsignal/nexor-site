---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { signalPosts } from '@/data/content';

export function getStaticPaths() {
  return signalPosts.map((post) => ({ params: { slug: post.slug }, props: { post } }));
}
const { post } = Astro.props;
---
<BaseLayout title={post.title} description={post.description} path={`/signal/${post.slug}/`}>
  <article class="container article">
    <div class="eyebrow">{post.source ?? 'Signal'} / {post.label}</div>
    <h1>{post.title}</h1>
    <p class="desc">{post.description}</p>
    <p class="meta">{post.date} · {post.readTime}</p>
    {post.body.map((paragraph) => <p>{paragraph}</p>)}
    <div class="article-nav">
      <a class="btn ghost" href="/signal/">← Back to Signal</a>
      <a class="btn" href="/support/">Support NEXOR SIGNAL</a>
    </div>
  </article>
</BaseLayout>
